import 'package:flutter/material.dart';

enum Gender {
  male,
  female,
}

const mainolor = Color(0xff0a0d22);
const accentColor = Color(0xffeb1555);
const cardColor = Color(0xff1d1f33);
const whiteColor = Colors.white;

const cardTextStyle =
    TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Colors.white);

const cardNumStyle =
    TextStyle(fontSize: 50, fontWeight: FontWeight.bold, color: Colors.white);
